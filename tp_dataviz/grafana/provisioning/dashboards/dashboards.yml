apiVersion: 1

providers:
  - name: "TP Dataviz"
    orgId: 1
    folder: "TP Dataviz"
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards   # 👈 on va monter ce chemin ci-dessous
      foldersFromFilesStructure: true